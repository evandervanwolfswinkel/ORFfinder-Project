//SQL queries
INSERT INTO orf
    (ORF_ID, ORF_start, ORF_stop, Sequentie_Sequentie_ID)
    VALUES
    (1, 15, 789, 1)
    
INSERT INTO sequentie
(Sequentie_ID, Sequentie_header, Sequentie)
VALUES (1, '>Dit is een test sequentie','ATCGTAGCT')


DELETE FROM orf
WHERE ORF_ID = 1;

DELETE FROM sequentie
WHERE Sequentie_ID = 1;

//connecting to the ORF database in python
import mysql.connector
conn = mysql.connector.connect(
    host = "hannl-hlo-bioinformatica-mysqlsrv.mysql.database.azure.com",
    user = "owe7_pg8@hannl-hlo-bioinformatica-mysqlsrv",
    db = "owe7_pg8")
cursor = conn.cursor()
//insert blast results into database
cursor.execute("""INSERT INTO sequentie
                (Sequentie_ID, Sequentie_header, Sequentie)
                VALUES ()
                INSERT INTO orf
                (ORF_ID, ORF_start, ORF_stop, Sequentie_Sequentie_ID)
                VALUES ()""")
cursor.close()
conn.close()

//connecting to the ORF database in python
import mysql.connector
conn = mysql.connector.connect(
    host = "hannl-hlo-bioinformatica-mysqlsrv.mysql.database.azure.com",
    user = "owe7_pg8@hannl-hlo-bioinformatica-mysqlsrv",
    db = "owe7_pg8")
cursor = conn.cursor()
//resultaten ophalen uit de database
cursor.execute("""SELECT *
                FROM sequentie
                WHERE Sequentie_header = """+headervariabelenaam+"""
                //stop het in een variabele om te printen
                SELECT *
                FROM orf
                WHERE ORF_ID = Sequentie_ID
                //stop het in een variabele om te printen
                """)
cursor.close()
conn.close()




